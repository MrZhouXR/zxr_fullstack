var getMovieInfo = function (movie) {
    return "\n      title: " + movie.title + ",\n      score: " + movie.score + ",\n      actor: " + movie.actor + ",\n      show\uFF1A " + movie.show + "\n      ";
};
